local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "javascript", "typescript", "java", "go", "terraform", },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
